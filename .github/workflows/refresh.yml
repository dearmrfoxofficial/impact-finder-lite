name: Refresh Charity Data

on:
  workflow_dispatch: {}   # lets you run it manually
  schedule:
    - cron: "0 6 * * 1"   # every Monday at 06:00 UTC

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps
        run: npm install node-fetch@3 csv-parse@5

      - name: Build data
        run: |
          mkdir -p data
          node scripts/build-data.mjs

        env:
          IRS_ACTIVE_URL: ${{ vars.IRS_ACTIVE_URL }}
          IRS_REVOCATIONS_URL: ${{ vars.IRS_REVOCATIONS_URL }}
          MAX_RECORDS: ${{ vars.MAX_RECORDS }}

      - name: Commit updated data
        run: |
          git config user.name "github-actions"
          git config user.email "actions@users.noreply.github.com"
          git add data/charities_us.json
          git commit -m "Update charity dataset" || echo "No changes"
          git push
